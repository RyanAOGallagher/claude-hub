# 2026-09-03

- [claude-hub] Fixed hub tab not starting: the hub path was pointing at a non-existent sibling (`~/Desktop/projects/_hub`), but the `_hub/` folder (briefs + log) lives inside the repo at `claude-hub/_hub/`. Commit e1bf24d had set config.json `hub` and the code fallbacks to the old sibling path without moving the folder. Reverted all hub resolution back to the in-repo `__dirname/_hub` per the recorded design: config.json `hub`, main.js loadConfig fallback + comments, renderer.js HUB_DIR, top-level CLAUDE.md refs, and README example/wording. Non-destructive (folder not moved). Restart the app (or the Hub tab) to pick it up.
- [claude-hub] Requested an in-app toggle for claude vs crush. Confirmed the backend already exists: `harness` key in `config.json` drives what command the pty runs (`main.js:181`), so it's just the terminal command string. Work queued in the brief: a `set-harness` IPC + small UI control, affecting new/resumed sessions only.
- [claude-hub] Implemented the harness toggle (uncommitted): `set-harness` IPC in main.js read-modify-writes `config.json` (preserves `groups`/`hub`, invalid values fall back to claude); sidebar row below Browser shows the active CLI (bot/zap icon, amber) and click flips claude ↔ crush; window title and notification copy follow via `harnessName()`; focus re-syncs the row from config so hand-edits still work. Affects new/resumed sessions only, running tabs keep their CLI. README updated, syntax-checked, handler logic smoke-tested. Restart the app to pick it up.
- [claude-hub] Harness theming per request: with crush active the orange accent becomes Crush's brand pink/purple. `--accent`/`--accent-grad` CSS vars flipped by a `data-harness` attribute on `<html>` (set in `paintHarnessRow`); crush values are the actual colors of Crush's logo (Dolly #ff60ff pink → Charple #6b50ff purple, pulled from charmbracelet/crush source + the charmtone palette), claude stays #e8b04b with an identical-stop gradient so it's visually unchanged. Gradient text on hub/harness rows + project-view header; solid accent on the active split button and xterm cursor (cursor read at tab creation). Status dots and blue info hues deliberately untouched. Syntax-checked; restart + toggle to see it.
- [claude-hub] Sidebar Terminal + Browser rows retinted from blue `#7fa8d9` to `#ddd` (the app's text white) per "blue doesn't fit anywhere"; row icons follow via currentColor. Left the other blues as-is: `.brief code` spans and the browser start-page `.portbtn` localhost buttons (separate affordances). Restart to see.